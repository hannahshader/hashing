#ifndef WORDLIST_H
#define WORDLIST_H

#include <string>
#include <iostream>
#include "wordNodeStruct.h"

using namespace std;

class WordList {
    public:

        // WordList(wordNode *node);
        // WordList();
        // wordNode *insertNodeReturnFront(wordNode *node);
        // ~WordList();
        // //Takes data from class, populates a node and adds it to the list
        // void insertWord(int lineNum, int lineContents_index, 
        //                 int filePath_index, std::string wordCaseSensitive);
        // wordNode *get_front();
        // wordNode *front = nullptr;
        // int list_length = 0;

    private:
        
};

#endif